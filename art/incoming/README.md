# Generated art, before processing

Drop generated PNGs here named `<slug>_NN.png`, then run:

```
npm run art:process
```

Sources are kept: they are the only copy of the pre-quantized image, and a
regeneration usually starts from adjusting one that was close.
